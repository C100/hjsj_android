<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.tworldapplication">

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".base.BaseActivity" />
        <activity android:name=".activity.main.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.main.MainActivity" />
        <activity android:name=".activity.card.CardMainActivity" />
        <activity android:name=".activity.admin.AdminMainActivity" />
        <activity android:name=".activity.account.AccountMainActivity" />
        <activity android:name=".activity.order.OrderMainActivity" />
        <activity android:name=".activity.home.HomeMainActivity"></activity>
    </application>

</manifest>